name: Forex Scalp Signal Scanner
on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:
jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: pip install yfinance pandas numpy
      - name: Run
        run: python signal_scanner.py
      - name: Save logs
        run: |
          git config --global user.name "bot"
          git config --global user.email "bot@github.com"
          git add open_signals.json closed_trades.csv signals_log.csv || true
          git add *.json *.csv || true
          git diff --staged --quiet || git commit -m "V4 $10 test update"
          git push || true
